<%= form_for work_order do |f| %>
  <fieldset>
    <legend>
      Work Order
    </legend>
    <ul>
      <div><%= work_order.sample_name %></div>
      <%= render work_order.template, form: f %>
    </ul>
    <%= f.submit %>
  </fieldset>
<% end %>